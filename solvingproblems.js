const express = [
    {name: 'mause', cost: 4000},
    {name: 'ddd', cost: 5000},
    {name: 'ttt', cost: 2000},
    {name: 'monitor', cost: 5000},
    {name: 'klava', cost: 6000},
    
];
let kop = express.filter(e => e.cost >= 4000);
console.log(kop);

let sum = 0; 
 express.forEach(function name(r) {
    sum += r.cost;
 });
console.log(sum);

//  let newmass = express.Map(product => product.name);
//  console.log(newmass);
function name() {
    

 let ifs = express.filter(product => product.cost < 5000);
 return z express.cost - (express.cost * 15) / 100;
}  

 